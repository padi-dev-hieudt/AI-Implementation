<Window x:Class="RBAC_WPF_2026.Views.PositionEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RBAC_WPF_2026.Views"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="350" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
        <local:InverseBoolConverter x:Key="InverseBoolConverter"/>
        
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FFE0E0E0"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="Background" Value="White"/>
        </Style>
        
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>
        
        <Style x:Key="ActionButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="20,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="2"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.4"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="White" CornerRadius="10" BorderBrush="#FFE0E0E0" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FF3498DB" CornerRadius="10,10,0,0" Padding="25,20">
                <StackPanel>
                    <TextBlock Text="{Binding WindowTitle}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                    <TextBlock Text="📍 Configure position details" FontSize="12" Foreground="#FFE8F4FD" Margin="0,5,0,0"/>
                </StackPanel>
            </Border>

            <!-- Form Content -->
            <ScrollViewer Grid.Row="1" Padding="25,20" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Position Name -->
                    <Label Content="Position Name:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                             Style="{StaticResource ModernTextBox}"
                             Padding="12,10" FontSize="14" Margin="0,0,0,20"/>

                    <!-- Is Active -->
                    <Label Content="Status:" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <CheckBox IsChecked="{Binding IsActive}" 
                              Content="Position is active" 
                              Style="{StaticResource ModernCheckBox}"
                              Margin="0,0,0,20"/>

                    <!-- Error Message -->
                    <Border Background="#FFE74C3C" CornerRadius="5" Padding="15,10" Margin="0,0,0,10"
                            Visibility="{Binding Message, Converter={StaticResource StringToVisibilityConverter}}">
                        <TextBlock Text="{Binding Message}" Foreground="White" FontWeight="SemiBold" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <!-- Action Buttons -->
            <Border Grid.Row="2" Background="#FFF8F9FA" CornerRadius="0,0,10,10" Padding="25,15" BorderBrush="#FFE0E0E0" BorderThickness="0,1,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Command="{Binding SaveCommand}" 
                            Content="{Binding SaveButtonText}"
                            Style="{StaticResource ActionButtonStyle}" Background="#FF27AE60" BorderBrush="#FF27AE60"
                            Padding="20,10" MinWidth="80" Margin="0,0,10,0"
                            IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                    <Button Command="{Binding CancelCommand}" 
                            Content="Cancel"
                            Style="{StaticResource ActionButtonStyle}" Background="#FF95A5A6" BorderBrush="#FF95A5A6"
                            Padding="20,10" MinWidth="80"/>
                </StackPanel>
            </Border>
            
            <!-- Loading Overlay -->
            <Grid Grid.RowSpan="3" Background="#80FFFFFF" 
                  Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <ProgressBar IsIndeterminate="True" Width="100" Height="6"/>
                    <TextBlock Text="Saving position..." Margin="0,15,0,0" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>