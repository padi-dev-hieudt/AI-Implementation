<UserControl x:Class="RBAC_WPF_2026.Views.PermissionManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Permission Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Add Permission" Command="{Binding AddPermissionCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FF4CAF50" Foreground="White" BorderBrush="#FF4CAF50"/>
            <Button Content="Edit Permission" Command="{Binding EditPermissionCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FF2196F3" Foreground="White" BorderBrush="#FF2196F3"/>
            <Button Content="Delete Permission" Command="{Binding DeletePermissionCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FFF44336" Foreground="White" BorderBrush="#FFF44336"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FFFF9800" Foreground="White" BorderBrush="#FFFF9800"/>
        </StackPanel>

        <!-- Permissions DataGrid -->
        <DataGrid x:Name="PermissionsDataGrid" Grid.Row="2" ItemsSource="{Binding Permissions}" 
                  SelectedItem="{Binding SelectedPermission}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FFE0E0E0"
                  AlternatingRowBackground="#FFF8F8F8"
                  SelectionMode="Single"
                  Tag="{Binding}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="45"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FF3498DB"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFF1F2F6"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Message -->
        <Border Grid.Row="3" Background="#FFF0F8FF" BorderBrush="#FFD0E0FF" BorderThickness="1" 
                Padding="10" Margin="0,15,0,0" CornerRadius="3">
            <TextBlock Text="{Binding Message}" Foreground="#FF1E3A8A" FontWeight="Medium"/>
        </Border>
    </Grid>
</UserControl>