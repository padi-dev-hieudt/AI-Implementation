<Window x:Class="RBAC_WPF_2026.Views.RoleEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RBAC_WPF_2026.Views"
        Title="{Binding Title}" Height="500" Width="450"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Window.Resources>
        <local:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="1*" MinHeight="100"/>
            <RowDefinition Height="Auto" MaxHeight="60"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Role Details Form -->
        <Grid Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Name:" Margin="0,0,10,5"/>
            <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,5" Padding="5"/>

            <Label Grid.Row="1" Grid.Column="0" Content="Description:" Margin="0,0,10,5"/>
            <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                     Margin="0,0,0,15" Padding="5" Height="60" AcceptsReturn="True" 
                     VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"/>

            <Label Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" Content="Permissions:" FontWeight="Bold" Margin="0,0,0,5"/>
        </Grid>

        <!-- Permissions Selection -->
        <GroupBox Grid.Row="1" Header="Assign Permissions" FontWeight="Bold" Margin="0,0,0,5" Padding="5">
            <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="180" Padding="0">
                <ItemsControl ItemsSource="{Binding AvailablePermissions}" Margin="0">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="White" BorderBrush="#DDDDDD" BorderThickness="1" 
                                    CornerRadius="4" Margin="2" Padding="5,3">
                                <CheckBox Content="{Binding Permission.Name}" IsChecked="{Binding IsSelected}" 
                                          FontSize="12" ToolTip="{Binding Permission.Description}">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="Foreground" Value="#2C3E50"/>
                                            <Style.Triggers>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter Property="Foreground" Value="#27AE60"/>
                                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <!-- Error Message -->
        <Border Grid.Row="2" Background="#FFFFE0E0" BorderBrush="#FFFF8080" BorderThickness="1" 
                Padding="10" Margin="0,5,0,5" CornerRadius="3" MaxHeight="50"
                Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibilityConverter}}">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFD00000" FontWeight="Medium" TextWrapping="Wrap"/>
            </ScrollViewer>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Save" Command="{Binding SaveCommand}" 
                    Padding="20,8" Margin="0,0,10,0" Background="#FF4CAF50" Foreground="White" BorderBrush="#FF4CAF50"
                    IsDefault="True"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                    Padding="20,8" Background="#FFF44336" Foreground="White" BorderBrush="#FFF44336"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>