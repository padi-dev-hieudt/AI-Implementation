<UserControl x:Class="RBAC_WPF_2026.Views.RoleManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:RBAC_WPF_2026.Views"
             mc:Ignorable="d">
    <UserControl.Resources>
        <local:PermissionsToStringConverter x:Key="PermissionsToStringConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Role Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button Content="Add Role" Command="{Binding AddRoleCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FF4CAF50" Foreground="White" BorderBrush="#FF4CAF50"/>
            <Button Content="Edit Role" Command="{Binding EditRoleCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FF2196F3" Foreground="White" BorderBrush="#FF2196F3"/>
            <Button Content="Delete Role" Command="{Binding DeleteRoleCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FFF44336" Foreground="White" BorderBrush="#FFF44336"/>
            <Button Content="Refresh" Command="{Binding RefreshCommand}" 
                    Padding="15,8" Margin="0,0,10,0" Background="#FFFF9800" Foreground="White" BorderBrush="#FFFF9800"/>
        </StackPanel>

        <!-- Roles DataGrid -->
        <DataGrid x:Name="RolesDataGrid" Grid.Row="2" ItemsSource="{Binding Roles}" 
                  SelectedItem="{Binding SelectedRole}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  IsReadOnly="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#FFE0E0E0"
                  AlternatingRowBackground="#FFF8F8F8"
                  SelectionMode="Single"
                  Tag="{Binding}">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Height" Value="45"/>
                    <Setter Property="Cursor" Value="Hand"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FF3498DB"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#FFF1F2F6"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="300"/>
                <DataGridTextColumn Header="Permissions" Binding="{Binding RolePermissions, Converter={StaticResource PermissionsToStringConverter}}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Message -->
        <Border Grid.Row="3" Background="#FFF0F8FF" BorderBrush="#FFD0E0FF" BorderThickness="1" 
                Padding="10" Margin="0,15,0,0" CornerRadius="3">
            <TextBlock Text="{Binding Message}" Foreground="#FF1E3A8A" FontWeight="Medium"/>
        </Border>
    </Grid>
</UserControl>